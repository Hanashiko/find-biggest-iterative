# Find Biggest (Iterative)

A simple Python script that finds the index of the largest element in an array using an iterative approach.

## Usage

```bash
python main.py
```

## How it works

The `find_biggest` function iterates through the array, tracking the largest value and its index, then returns the index of the maximum element.
